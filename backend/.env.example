# ─────────────────────────────────────────────────────────────────────
#  AI Image Generator — Configuration
# ─────────────────────────────────────────────────────────────────────

# Server port
PORT=8100

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Models to preload at startup (comma-separated)
# Options: lightning, realvis_fast, realvis_quality, flux, hunyuan_image
# Set to "" to skip preloading (lazy load on first request)
PRELOAD_MODELS=lightning,realvis_fast,realvis_quality,flux

# HuggingFace cache directory (inside container)
HF_HOME=/data/models

# Generated images directory (inside container)
GENERATED_DIR=/data/generated
