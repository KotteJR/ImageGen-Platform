# AI Image Generator — Python dependencies
# PyTorch + CUDA are provided by the NGC base image (DO NOT install torch here)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# AI / Diffusion (compatible with NGC PyTorch 25.01 / PyTorch 2.6)
# HunyuanVideoPipeline available since diffusers 0.31.0
diffusers==0.32.0
transformers==4.47.1
accelerate==1.2.1
safetensors>=0.4.5
sentencepiece>=0.2.0

# Image processing
Pillow>=10.4.0

# Professional graphics rendering (charts, dashboards, infographics)
matplotlib>=3.8.0

# HuggingFace Hub
huggingface-hub>=0.26.0

# Pydantic (data validation)
pydantic>=2.9.0

# TTS — Chatterbox (Resemble AI, 500M params)
# Installed with --no-deps because chatterbox pins numpy<1.26 and torch==2.6.0
# which conflict with the NGC image (numpy 1.26.4, torch 2.6.0a0). All work fine.
soundfile>=0.12.0
scipy>=1.11.0

# NOTE: xformers is NOT needed — PyTorch 2.x has built-in SDPA attention
# NOTE: torch is NOT listed — provided by the NGC base image
